export enum UserStatus {
  ACTIVATED = `Activated`,
  ARCHIVED = `Archived`,
  DISABLED = `Disabled`,
}

export enum RequestStatus {
  ACCEPTED = 'Accepted',
  DECLINED = 'Declined',
  WAITING = 'Waiting',
}

export enum MenuAction {
  VIEW = `view`,
  EDIT = `edit`,
  DELETE = `delete`,
}
